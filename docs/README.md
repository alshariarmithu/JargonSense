# Research archive

This directory preserves the original proposal, project plan, final report,
and generated preprocessing examples. They document the experiments used to
select `tfidf13_svm`, including the representation and classifier
alternatives that were discarded along the way.

The runnable, reduced SE pipeline is documented in the repository root
`README.md`.
